Using buzzfeed's API, this script intakes user input to pull a list of titles and article descriptions of articles. 
Then the program allows the user to enter one of the articles that interests them and sends the user to the URL page.
