Using BuzzFeed's API, this script intakes user input from the command line to pull a list of titles and article descriptions that fit the user's preferences. 
Then the program allows the user to enter one of the article titles and sends the user to the URL page.
